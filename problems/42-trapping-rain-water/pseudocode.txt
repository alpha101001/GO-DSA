PROBLEM: 42. Trapping Rain Water
LINK: https://leetcode.com/problems/trapping-rain-water/description/
FUNCTION: func trap(height []int) int

----------------------------------
--------------CODE----------------
----------------------------------

given h[]

left, right, leftMax, rightMax, total := 0,len(h)-1,0,0,0

for left<right:
  if h[left] <= h[right]:
    if h[left]>leftMax:
      leftMax=h[left]
    else:
       total+= leftMax - h[left]
    left++
  else:
    if h[right] > rightMax:
      rightMax = h[right]
     else:
        total += rightMax-h[right]
    right--

return total
----------------------------------
--------------CODE----------------
----------------------------------



----------------------------------
--------------TEST CASES----------
----------------------------------

Example 1: Example 1
Call: trap([]int{0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1})
Expected: 6
Explanation: The above elevation map (black section) is represented by array [0,1,0,2,1,0,1,3,2,1,2,1]. In this case, 6 units of rain water (blue section) are being trapped.

Example 2: Example 2
Call: trap([]int{4, 2, 0, 3, 2, 5})
Expected: 9

